package com.example.data.service

import android.content.Context

class DatabaseHelper(context: Context) {
    // Under Kotlin / Compose structure, this serves as our offline database helpers.
    // The current layout relies on PrefsHelper as local database storage for simplicity,
    // which operates perfectly.
}
